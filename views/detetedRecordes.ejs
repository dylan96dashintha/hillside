<!DOCTYPE html>
<html>


<head>
    <link rel="stylesheet" href="https://stackpath.bootstrapcdn.com/bootstrap/4.4.1/css/bootstrap.min.css"
        integrity="sha384-Vkoo8x4CGsO3+Hhxv8T/Q5PaXtkKtu6ug5TOeNV6gBiFeWPGFN9MuhOf23Q9Ifjh" crossorigin="anonymous">
    <script src="https://stackpath.bootstrapcdn.com/bootstrap/4.4.1/js/bootstrap.min.js"
        integrity="sha384-wfSDF2E50Y2D1uUdj0O3uMBJnjuUD4Ih7YwaYd1iqfktj0Uod8GCExl3Og8ifwB6" crossorigin="anonymous">
    </script>
</head>

<body>

    <nav class="navbar navbar-expand-lg navbar-dark bg-dark mb-4">
        <a class="navbar-brand" href="#">HillSide</a>
        <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarNavDropdown"
            aria-controls="navbarNavDropdown" aria-expanded="false" aria-label="Toggle navigation">
            <span class="navbar-toggler-icon"></span>
        </button>
        <div class="collapse navbar-collapse" id="navbarNavDropdown">
            <ul class="navbar-nav">
                <li class="nav-item active">
                    <a class="nav-link" href="./">Home <span class="sr-only">(current)</span></a>
                </li>
                <%if(empDetails.empType === "owner"){%>
                <li class="nav-item">
                    <a class="nav-link" href="/editRoomDetails">Change Room Details</a>
                </li>
                <%}%>
                <li class="nav-item">
                    <a class="nav-link" href="/logout">Log Out</a>
                </li>
            </ul>
        </div>
        <span class="navbar-text">
            Logged In: <%=empDetails.name%>
        </span>
    </nav>
    <div class="text-center">
        <button><a href="createNewRecord" type="button" class="btn btn-primary btn-lg">Create a New Order</a></button>
    </div>
    <table class="table table-striped table-dark">
        <thead>
            <tr>
                <th scope="col">OrderId</th>
                <th scope="col">First Name</th>
                <th scope="col">Last Name</th>
                <th scope="col">Address</th>
                <th scope="col">Mobile Number</th>
                <th scope="col">pavement</th>
            </tr>
        </thead>
        <tbody>
            <% for(var i=0; i <count; i++) { %>
            <tr>
                <td><%= orderId[i] %></td>
                <td><%= firstname[i] %></td>
                <td><%= lastname[i] %></td>
                <td><%= address[i] %></td>
                <td><%= mobilenum[i] %></td>
                <td><%=pavement[i] %></td>
                <td>
                    <form action="/pavement" method="POST">
                        <button type="submit" class="btn btn-success" value="<%= orderId[i] %>" name="pay">Pay</button>
                    </form>
                </td>
                <td>
                    <form action="/deleteRecord" method="POST">
                        <button type="submit" class="btn btn-danger" value="<%= orderId[i] %>"
                            name="del">Delete</button>
                    </form>
                </td>
                <td>
                    <form action="/viewOrder" method="POST">
                        <button type="submit" class="btn btn-danger" value="<%= orderId[i] %>"
                            name="order">View</button>
                    </form>
                </td>
            </tr>
            <% } %>
        </tbody>
    </table>
</body>

</html>